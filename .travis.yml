language: ruby
rvm:
- 2.1.1
before_script:
  - 'npm install -g istanbul && npm install -g grunt-cli && npm install grunt-coveralls'
script:
  - bundle exec rake teaspoon
after_script:
  - 'grunt coveralls:teaspoon_coverage'